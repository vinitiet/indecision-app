console.log('utils.js is running')

export const square = (x) => x * x;

export const add = (a, b) => a + b;

// const substract = ;

export default (a, b) => a - b;

export const a = 4;
// export { a, square, add, substract as default }
